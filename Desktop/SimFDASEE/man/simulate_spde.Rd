% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_spde.R
\name{simulate_spde}
\alias{simulate_spde}
\title{Simulate BMTSM Functional Data with Optional Jumps}
\usage{
simulate_spde(
  nx = 101,
  nt = 101,
  sigma = 1,
  lambda = 2,
  kappa = 0.5,
  kernel = function(h) exp(-abs(h))
)
}
\arguments{
\item{nx}{Integer. Number of spatial grid points. Default is 101.}

\item{nt}{Integer. Number of time points. Default is 101.}

\item{sigma}{Numeric. Scaling factor for the covariance kernel of the continuous part. Default is 1.}

\item{lambda}{Numeric. Rate parameter for the exponential distribution controlling jump times. Default is 2.}

\item{kappa}{Numeric. Variance parameter for jump magnitudes. Default is 0.5.}

\item{kernel}{Function. Stationary covariance function for the continuous part. Should accept a single argument \(h = x - y\). Default is exponential \code{function(h) exp(-abs(h))}.}
}
\value{
A list with the following elements:
\describe{
\item{cont_samples}{Matrix of continuous functional samples (nt × nx).}
\item{discont_samples}{Matrix including the jump component (nt × nx).}
\item{locs}{Vector of time indices where jumps occur.}
}
}
\description{
Generates a matrix of functional data over space and time, combining a continuous
part generated from a stationary covariance kernel and an optional discontinuous
jump component.
}
\details{
The continuous part of the data is generated via a stationary covariance kernel.
The jump component is generated at random times from a Poisson-like process,
with jump magnitudes drawn from a normal distribution with variance \code{kappa}.
The covariance function can be customized to simulate different correlation structures.
}
\examples{
# Default simulation with exponential kernel
sim <- simulate_spde()

# Simulation with Gaussian kernel
sim2 <- simulate_spde(kernel = function(h) exp(-h^2))

}
