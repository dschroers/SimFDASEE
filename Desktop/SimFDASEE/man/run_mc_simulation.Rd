% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mc_simulations.R
\name{run_mc_simulation}
\alias{run_mc_simulation}
\title{Run Monte Carlo Simulation for BMTSM Functional Data}
\usage{
run_mc_simulation(
  K = 10000,
  nx = 101,
  nt = 101,
  kernel = function(h) exp(-abs(h)),
  sigma = 1,
  lambda = 2,
  kappa = 0.5,
  Delta = 0.01,
  sd_trunc = 3,
  rho = 0.95
)
}
\arguments{
\item{K}{Integer. Number of Monte Carlo iterations. Default is 10000.}

\item{nx}{Integer. Number of spatial/grid points. Default is 101.}

\item{nt}{Integer. Number of temporal points (observations). Default is 101.}

\item{kernel}{Function. Covariance kernel function taking a lag \code{h} as input. Default is exponential kernel: \code{function(h) exp(-abs(h))}.}

\item{sigma}{Numeric. Scale parameter for the simulation. Default is 1.}

\item{lambda}{Numeric. Jump intensity parameter for the simulation. Default is 2.}

\item{kappa}{Numeric. Jump size parameter for the simulation. Default is 0.5.}

\item{Delta}{Numeric. Grid spacing for truncation procedure. Default is 0.01.}

\item{sd_trunc}{Numeric. Standard deviation multiplier for truncation. Default is 3.}

\item{rho}{Numeric. Variance-explained threshold for effective dimension calculations. Default is 0.95.}
}
\value{
A list with the following components:
\describe{
\item{results}{A data frame containing relative errors and effective dimensions for different estimation methods: \code{rcv}, \code{sarcv}, \code{trunc_rcv}, \code{trunc_sarcv}. Each entry reports median with 0.25 and 0.75 quantiles in the format \code{median (Q1, Q3)}.}
\item{d_true}{Effective dimension of the true covariance kernel based on \code{rho}.}
\item{q}{The true covariance matrix used in the simulation.}
}
}
\description{
This function performs a Monte Carlo simulation to evaluate different
covariance estimation methods for BMTSM (Brownian motion-type stochastic
models) functional data, optionally with jumps. It computes relative
errors of the estimated covariance matrices and effective dimensions
for multiple methods (raw, shifted/adjusted, and truncated versions).
}
\examples{
# Run a small simulation with 10 iterations and a Gaussian kernel
gaussian_kernel <- function(h) exp(-h^2)
sim_res <- run_mc_simulation(K = 10, kernel = gaussian_kernel, nx = 50, nt = 51)
print(sim_res$results)

}
